
/*import { createApp } from 'vue'
import draggable from 'vuedraggable'
import MenuBuilder from './components/MenuBuilder.vue'
import MenuItem from './components/MenuItem.vue'
import PrimeVue from 'primevue/config';
import ToastService from 'primevue/toastservice';
const initializeVue = () => {

    const container = document.getElementById('menu-builder-container')

    if (container) {
        const app = createApp({
            components: {
                MenuBuilder,
                MenuItem,
                draggable,
            }
        })
        app.use(PrimeVue)
        app.use(ToastService)
        app.mount(container)
    }
}

document.addEventListener('livewire:init', initializeVue)
document.addEventListener('DOMContentLoaded', initializeVue)
*/

