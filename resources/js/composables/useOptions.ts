import {
    breakpoints,
    sides,
    useHeadingTagOptions,
    useAuthorOptions,
    useAuthors,
    useBgAttachmentOptions,
    useBgColorOptions,
    useBgPositionOptions,
    useBgSizeOptions,
    useBorderColorOptions,
    useBorderRadiusOptions,
    useBorderSizeOptions,
    useBorderStyleOptions,
    useButtonColorOptions,
    useButtonSizeOptions,
    useCategories,
    useCategoryOptions,
    useColorOptions,
    useColors,
    useColspanOptions,
    useContainerTypeOptions,
    useFontSizeOptions,
    useFontStyleOptions,
    useFontWeightOptions,
    useGapOptions,
    useGridColsOptions,
    useMarginOptions,
    usePaddingOptions,
    useShadowColorOptions,
    useShadowSizeOptions,
    useSizeOptions,
    useSortOptions,
    useTagOptions,
    useTags,
    useTextAlignOptions,
    useTextColorOptions,
    useTextTransformOptions,
    useThemeOptions,
    useUserOptions,
    useUsers,
} from "@/composables/options";
export const useOptions = () => {
    return {
        breakpoints,
        sides,
        headingTagOptions: useHeadingTagOptions(),
        textColorOptions: useTextColorOptions(),
        textAlignOptions: useTextAlignOptions(),
        fontSizeOptions: useFontSizeOptions(),
        fontWeightOptions: useFontWeightOptions(),
        fontStyleOptions: useFontStyleOptions(),
        textTransformOptions: useTextTransformOptions(),
        bgColorOptions: useBgColorOptions(),
        bgSizeOptions: useBgSizeOptions(),
        bgPositionOptions: useBgPositionOptions(),
        bgAttachmentOptions: useBgAttachmentOptions(),
        sizeOptions: useSizeOptions(),
        categoryOptions: useCategoryOptions(),
        tagOptions: useTagOptions(),
        authorOptions: useAuthorOptions(),
        userOptions: useUserOptions(),
        sortOptions: useSortOptions(),
        containerTypeOptions: useContainerTypeOptions(),
        gridColsOptions: useGridColsOptions(),
        gapOptions: useGapOptions(),
        colspanOptions: useColspanOptions(),
        marginOptions: useMarginOptions(),
        paddingOptions: usePaddingOptions(),
        borderColorOptions: useBorderColorOptions(),
        borderSizeOptions: useBorderSizeOptions(),
        borderStyleOptions: useBorderStyleOptions(),
        borderRadiusOptions: useBorderRadiusOptions(),
        shadowSizeOptions: useShadowSizeOptions(),
        shadowColorOptions: useShadowColorOptions(),
        buttonSizeOptions: useButtonSizeOptions(),
        buttonColorOptions: useButtonColorOptions(),
        themeOptions: useThemeOptions(),
    };
};
